# Publication Scope

This public package contains the generic continuity engine, dashboard shell,
MCP adapter, candidate review machinery, and local-first documentation.

It intentionally excludes private vault data, project-specific seeds, runtime
reports, tunnel state, cloud sync payloads, screenshots, and personal
relationship history.

Public examples should use synthetic names and synthetic memory nodes only.
